#!/usr/bin/env ruby

$: << File.expand_path(File.dirname(__FILE__) + "/../lib")

require 'overwatch/cli'
require 'overwatch/cli/command'

args = ARGV.dup
ARGV.clear

command = args.shift.strip rescue 'help'

Overwatch::CLI.load
Overwatch::CLI.run(command, args)